spring:
  application:
    name: demo  # ?????????

# Ollama ??????
ai:
  ollama:
    base-url: http://localhost:11434
    chat:
      model: qwen2.5:7b  # ???? qwen2.5:7b ? qwen2.5:14b?function calling ????
      options:
        temperature: 0.7
        max-tokens: 1000
    client:
      connect-timeout: 5s
      read-timeout: 30s
      max-attempts: 3
      backoff:
        initial-interval: 1s
        max-interval: 5s
        multiplier: 1.5

spring:
  application:
    name: ai-weather-assistant

server:
  port: 8080

# Spring AI 1.0.3 ??
spring:
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: qwen2.5:7b  # ??????? `ollama pull qwen2.5:7b` ??
        options:
          temperature: 0.7
          max-tokens: 1000
      client:
        connect-timeout: 5s
        read-timeout: 30s

# ??API?????????UAPIs?
uapis:
  api-key: ${UAPIS_API_KEY:uapi-bvnqejbzlbNnG8LKaxANzGaTA09JDnAI7948HOUQ}
  base-url: https://uapis.cn/api
  weather-path: /misc/weather